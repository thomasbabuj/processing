// Rotate Rect 

void setup(){
	size( 600, 600);

	// Stage visual representation
	// Created rect with the same size of the Window 

	rotate( radians( 10));
	noFill();
	rect(0, 0, width, height);

	rotate( radians( 10));
	noFill();
	rect(0, 0, width, height);

	rotate( radians( 10));
	noFill();
	rect(0, 0, width, height);

	rotate( radians( 10));
	noFill();
	rect(0, 0, width, height);

	rotate( radians( 10));
	noFill();
	rect(0, 0, width, height);

}

